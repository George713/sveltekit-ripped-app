import ArrayComponent from './ArrayComponent.svelte';
import Background from './Background.svelte';
import Wrapper from './Wrapper.svelte';

export const Array = {
    ArrayComponent,
    Background,
    Wrapper
};
